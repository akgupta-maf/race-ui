// Shared auth response/domain types. Re-exported from the existing race-ui
// service types so every app has one definition.
export type {
  Permission,
  Role,
  Country,
  UserDetails,
  LoginResponse,
} from '../services/auth.response';
